# Jenkins Docker CI/CD Project

Created a CI/CD pipeline using Jenkins and Docker on AWS EC2. Installed Jenkins and Docker, configured Docker permissions for Jenkins, connected GitHub repository, and automated application build and deployment using Jenkins Pipeline. The application was successfully deployed in a Docker container and accessed through port 8000.

bash
sudo usermod -aG docker jenkins

